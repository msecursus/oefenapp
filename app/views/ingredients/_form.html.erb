<%= simple_form_for(@ingredient) do |i| %>
  <%= i.error_notification %>

  <div class="form-inputs">
    <%= i.input :name %>
   
  </div>

  <div class="form-actions">
    <%= i.button :submit, class: "formbtn" %>
  </div>
<% end %>