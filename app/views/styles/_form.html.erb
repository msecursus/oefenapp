<%= simple_form_for(@style) do |s| %>
  <%= s.error_notification %>

  <div class="form-inputs">
    <%= s.input :name %>
   
  </div>

  <div class="form-actions">
    <%= s.button :submit, class: "formbtn" %>
  </div>
<% end %>